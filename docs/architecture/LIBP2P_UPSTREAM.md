# libp2p fork (retired)

> **Superseded:** The in-tree cpp-libp2p fork was deleted after absorbing PeerId derivation into [`foundation/identity/`](../../src/foundation/identity/). See [MESH_IDENTITY.md](MESH_IDENTITY.md) and [MESH.md](MESH.md).

Product mesh underlay is Amp (`pp-cpp-amp`). Host / TCP / Yamux / Noise were removed earlier (A017).
