pp_browser_add_base_library(pp_base_ui
  SOURCES
    ChatFormHelper.cpp
    ContextMenuHost.cpp
    EmojiCatalog.cpp
    InputCoordinator.cpp
    RmlVariantHelpers.cpp
    Theme.cpp
    ViewCatalog.cpp
  PUBLIC_LIBS
    pp_base_platform
    pp_base_i18n
    pp_base_render
    RmlUi::Core
    nlohmann_json::nlohmann_json)

if(PP_BROWSER_BUILD_TESTS)
  add_subdirectory(tests)
endif()
