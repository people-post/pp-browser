#pragma once

namespace pbr {

/** Shared chrome apply ops for surface→shell bridges (contacts / chat / people-picker). */
enum class ShellChromeOp {
  None,
  DirtyNav,
  SyncLayout,
};

} // namespace pbr
