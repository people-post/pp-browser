#pragma once

namespace pbr {

/** Well-known default PIN for auto-provisioned vaults. Weak — users should change in Me → Security. */
inline constexpr const char* kDefaultProfilePin = "123456";

} // namespace pbr
